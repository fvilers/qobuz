interface Award {
  award_id: string;
  award_slug: string;
  awarded_at: number;
  name: string;
  publication_id: string;
  publication_name: string;
  publication_slug: string;
  slug: string;
}

export default Award;
